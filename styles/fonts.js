import { Syne, Suez_One } from '@next/font/google';

export const syne = Syne({
 subsets: ['latin'],
 weight: ['700', '600', '400'],
});

export const suez = Suez_One({
 subsets: ['latin'],
 weight: '400',
});
